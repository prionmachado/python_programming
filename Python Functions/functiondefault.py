def cal_prod(a=2,b=3):    #you can give default values in the parameters if arguments are not there
    print(a*b)
    return a*b

cal_prod()      # but if you give arguments then arg will be executed not default parameters
